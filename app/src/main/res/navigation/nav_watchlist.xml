<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_watchlist"
    app:startDestination="@id/watchlistFragment">
    <fragment
        android:id="@+id/watchlistFragment"
        android:name="com.azizutku.movie.presentation.screens.watchlist.WatchlistFragment"
        android:label="@string/title_fragment_watchlist">
        <action
            android:id="@+id/action_watchlistFragment_to_movieFragment"
            app:destination="@id/movieFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />
        <deepLink
            app:uri="movieapp://watchlist/" />
    </fragment>
    <fragment
        android:id="@+id/movieFragment"
        android:name="com.azizutku.movie.presentation.screens.movie.MovieFragment"
        android:label="@string/title_fragment_movie"
        tools:layout="@layout/fragment_movie">
        <argument
            android:name="movieId"
            app:argType="integer" />
    </fragment>
</navigation>
